# Python Telemutter Envelope

Pure Python 3 stdlib implementation of the same minimal envelope protocol used by the Rust crate.

## Run tests

```bash
python -m unittest discover -s python/tests -v
```
